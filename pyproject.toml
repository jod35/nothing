[project]
name = "fastapi-github-actions"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "pytest>=9.0.3",
    "ruff>=0.15.13",
]

[dependency-groups]
dev = [
    "ruff>=0.15.13",
]
